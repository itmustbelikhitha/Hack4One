{
  "name": "dayflow",
  "version": "1.0.0",
  "private": true,
  "description": "DAYFLOW - Human Resource Management System",
  "workspaces": ["apps/api", "apps/web"],
  "scripts": {
    "dev": "npm run dev -w apps/api & npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run test -w apps/api && npm run test -w apps/web",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/web",
    "format": "prettier --write .",
    "migrate": "npm run prisma:migrate -w apps/api",
    "seed": "npm run prisma:seed -w apps/api"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
